# ========================================================
# MongoDB Docker (Localhost) Environment Configuration
# ========================================================

# เชื่อมต่อกับ MongoDB ผ่าน Docker Container (พอร์ต 27017)
# เริ่มรันคอนเทนเนอร์ด้วย: docker compose up -d mongo
MONGODB_URI=mongodb://127.0.0.1:27017/portfolio_system
MONGODB_DB=portfolio_system

AUTH_SECRET=pfs-prod-secret-992384729
ADMIN_EMAIL=admin@kmitl.ac.th
ADMIN_PASSWORD=Admin@1234
NEXT_PUBLIC_APP_URL=http://localhost:3000
